name: cakue_server
description: "Cakue Backend API — Dart/Shelf server connecting Flutter Web to MySQL"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  mysql_client: ^0.0.27
  uuid: ^4.4.2

dev_dependencies:
  lints: ^4.0.0
